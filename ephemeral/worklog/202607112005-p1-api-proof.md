# P1 API Proof Session

started: 2026-07-11 20:05 CST
worktree: `/Users/tyler/src/merger-herder-validation/.worktrees/p1-api-proof`
branch: `codex/p1-api-proof`

## Goal

Build the first executable DTU GitHub slice for the agreed P1 REST surface,
prove it across real HTTP and Git transports, obtain Claude/Codex consensus on
the implementation and proof, and publish a pull request.

## Constraints And Decisions

- correction: Internal fields default to concrete values. Pointers are used
  only for shared state, lifecycle ownership, no-copy types, or external APIs
  that require them. URL fields on the public instance are `url.URL` values.
- decision: `testing/synctest` is useful for isolated timer tests but cannot
  drive the real-listener proof because network I/O is not durably blocked.
- decision: The public GitHub/Git listener and private test-control listener
  are separate. Tests seed and inspect state only through control HTTP.
- decision: The first tracer bullet mints a token with `go-github`, uses that
  exact token for real Git smart HTTP and `PullRequests.Get`, mutates a real
  ref, observes the new PR snapshot, advances virtual time, and proves expiry.
- decision: OAuth remains excluded from this P1 slice.

## Session Activity

- command: inspected repository status and contracts; repository initially had
  the agreed endpoint claims and three prior consensus rounds uncommitted on
  `main`.
- command: created `codex/p1-api-proof` and committed those existing artifacts
  as `13f2b19 docs(api): define P1 endpoint claims`.
- command: restored the root checkout to `main` and created this task worktree.
- skill_use: `session-worklog` source=pagerguild/core-tools -> preserve design,
  commands, proof, review, and PR state.
- skill_use: `claude-codex-consensus` source=pagerguild/core-tools -> required
  adversarial implementation and proof review after the build.
- doc_lookup: Go 1.26.5 `testing/synctest` package documentation -> real network
  I/O and external processes are outside the fake-clock bubble's useful scope.
- implementation: added a Go 1.26 module, `dtu` runtime package, separate public
  and control listeners, `cmd/dtu-github`, RSA App JWT verification, scoped
  installation tokens, targeted PR reads, and Git CGI backed by real bare repos.
- implementation: public `Instance` stores `url.URL` values; shared runtime and
  HTTP client pointers are retained only where lifecycle/no-copy sharing is
  intentional.
- proof: `TestP1APIGoldenJourney` passes across real REST, control, and Git
  listeners; it observes real-ref movement, retained deleted-head state, and
  virtual token expiry without sleeping.
- proof: `TestP1APINegativeClaims` covers the contracted JWT, installation,
  selector, permission, scope, error, and unsupported-request matrix.
- proof: `TestExecutableStartup` builds and launches the actual command, reads
  readiness, calls control HTTP, and observes clean signal shutdown.
- correction: Codex self-review found that trimming both path ends accepted a
  trailing slash contrary to `HTTP-01`; routing now removes only the required
  leading slash and a regression assertion covers the boundary.
- correction: Codex self-review found that control owner `..` could escape the
  repository root; path components now reject empty, dot, dot-dot, slash, and
  backslash values.
- command: `go vet ./...` passed.
- command: `go test -race -count=1 ./...` passed.
- command: `git diff --check` passed.
- review: Claude round 1 found no critical/bug/race issues and raised three
  design proof questions plus three nits.
- review_resolution: rejected the claim that `contents:read` cannot authorize
  targeted PR reads; current GitHub primary documentation explicitly lists
  either Pull Requests read or Contents read for this endpoint. Accepted the
  missing isolated-branch proof and added successful REST reads for each.
- review_resolution: accepted filesystem-dependent Git casing; requests are
  now rewritten to the authorized repository's canonical path before
  `git-http-backend`, with a mixed-case Git request in the network proof.
- review_resolution: accepted missing raw token-success proof, subprocess
  public-listener coverage, rejected-request mutation coverage, and subprocess
  work under the world lock. All four were corrected.
- external_resource: https://docs.github.com/en/rest/pulls/pulls#get-a-pull-request
  -> current endpoint permissions list Pull Requests read or Contents read.
- command: after round-1 revisions, `go test -race -count=1 ./...`,
  `go vet ./...`, and `git diff --check` passed.
- review: Claude round 2 independently confirmed GitHub's two permission
  alternatives, adjudicated every round-1 finding as resolved, found no
  critical/bug/design/race issue, and declared the bounded proof
  consensus-passed.
- review_resolution: implemented the highest-value optional round-2 finding by
  proving a `contents:read` token cannot push through `git-receive-pack`.
- review_resolution: updated Git case-insensitivity evidence and rejected
  ambiguous same-name repositories within one installation.
- decision: do not duplicate the complete golden journey in the subprocess
  test; the executable test owns command/readiness/listener/shutdown wiring,
  while the same exported runtime receives the load-bearing in-process network
  proof.

## Proof And Closeout

- consensus: Codex self-review plus two Claude Opus rounds; round 2 reported no
  critical, bug, design, or race findings and marked the bounded proof complete.
- proof: `go vet ./...` passed.
- proof: `go test -race -count=1 ./...` passed; `dtu` completed in 3.622s.
- proof: `govulncheck ./...` reported no vulnerabilities.
- proof: `git diff --check` passed and changed Markdown was formatted with
  `vp fmt`.
- remaining: live-GitHub conformance for provisional statuses and the complete
  MergeHerder/Octokit P1 system scenario are explicitly outside this PR.
- branch_pr: implementation committed as `264e6b9`, branch
  `codex/p1-api-proof` pushed, and PR
  `https://github.com/tylergannon/merger-herder-validation/pull/1` opened ready
  for review against `main`.
- pr_state: PR #1 reported `OPEN`, `MERGEABLE`, and no configured status checks
  at head `264e6b976f333e108b97bf41a111f92b44c1c702` before this worklog-only
  closeout commit.
